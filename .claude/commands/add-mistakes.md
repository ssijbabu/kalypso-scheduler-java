Review the work done in this session (git log, DAY*_SUMMARY.md files, and any errors encountered) and identify concrete mistakes that were made — wrong API calls, wrong types, wrong assumptions, wrong tool choices, wrong ordering of steps, etc.

Then add them to the "## Known Mistakes to Avoid" section of CLAUDE.md (create the section if it doesn't exist). Each entry must follow this format:
- **What was done wrong**: one sentence describing the incorrect action
- **Why it failed**: one sentence on the root cause
- **Correct approach**: one sentence on what to do instead

Only add mistakes that are non-obvious and would trip up a future Claude instance working in this codebase. Do not add generic Java or Maven advice. Focus on mistakes specific to this project's libraries, versions, and patterns.

After updating CLAUDE.md, confirm what was added.
